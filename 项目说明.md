# 加密编年史 · 纂修条例

> 本条例按「条例」分列：一条讲清一件事。读条例 01 即知全貌。

## 条例 01 · 缘起（这个作品是什么）

**Crypto Timeline Agent** 是一个零依赖的加密货币时间线资讯 Agent。它做两件事：

1. **收藏**：内置 10 个主流币种、93 条历史大事件（人工整理自公开资料），按分类与影响方向标注；
2. **校勘**：用**币安官方开源数据仓库的历史日线**给每条事件配上「事件当日的官方行情与后续反应」，
   并按事件性质与分类给出史评统计。

交付三种形态：

| 形态 | 入口 | 给谁用 |
|---|---|---|
| **编年史（命令行）** | `python agent.py chronicle BTC` | 给 Agent 与评委：按年份分卷的事件长卷，含行情与史评 |
| 网页控制台「链事纪」 | `python agent.py web`（浏览器打开 127.0.0.1:8000） | 想看图表的人（市场总览 / 时间线 / 资讯 / 报告四面板） |
| 报告文件 | `python agent.py report BTC`（HTML + Markdown） | 想要一份可分享文件的人 |

## 条例 02 · 体例（编年史的结构）

```
卷首    底本说明 + 取数通道 + 当期行情
卷 · 年 按年份分卷
  条目    纪年 · 事件名
          〔分类〕〔影响方向〕
          事件简介
          〔市场反应〕 交易日 · 收盘 · 当日涨跌 · 后 5 个交易日 · 后 30 个交易日 · 距区间高点
史评    按事件性质与分类的统计（平均反应、30 日上涨占比）
凡例    口径与边界（固定五条）
```

## 条例 03 · 底本（数据来源）

| 数据 | 来源 | 是否官方 |
|---|---|---|
| 历史行情（每条事件的市场反应） | 币安官方开源数据仓库 `data.binance.vision` 的**现货月度日线归档** | **官方** |
| 当期行情 | 币安官方公开行情入口 `data-api.binance.vision`（多源容灾备用 Gate.io / OKX / CoinLore / …） | **官方优先** |
| 走势图（报告） | 官方公开行情入口 `/api/v3/klines` | **官方** |
| 历史事件 | 人工整理自公开资料（`data/events.json`） | 人工整理 |
| 新闻资讯 | 各媒体公开 RSS | 第三方 |

归档路径：`/data/spot/monthly/klines/<PAIR>/1d/<PAIR>-1d-<YYYY-MM>.zip`，一个月一个文件。

## 条例 04 · 四路取数（通道）

| 取数方式 | 命令 | 当期行情来源 | 需要上网工具 |
|---|---|---|---|
| 常规 | `chronicle BTC` | 币安官方公开行情优先，多源容灾 | 不需要 |
| 重跑 | `chronicle BTC --live` | 同上，强制重新选路 | 不需要 |
| 官方小工具 | `chronicle BTC --skill` | 官方 CLI 直取官方公开行情 | 需要（本机走代理时自动传给 CLI） |
| 纯档案 | `chronicle BTC --official` | 官方归档最近交易日（T+1） | **不需要** |

**历史行情四条通道统一走官方归档** —— 归档是 ZIP 文件，不适合由 CLI 取，这一点如实说明。

## 条例 05 · 口径（怎么算的）

1. **事件日**：事件发生当天；当天无交易数据时，取其后第一个交易日，并注明顺延天数；
2. **当日涨跌** = 当日收盘 ÷ 前一交易日收盘 − 1；
3. **后续反应** = 以事件当日收盘为基准，第 5 / 30 个**交易日**的累计涨跌（非自然日）；
4. **距区间高点** = 当日收盘相对「至当日为止的归档区间最高价」的回撤；
5. **史评** = 各分组事件上述指标的**简单平均**与 30 日上涨占比，样本量印在括号里；
6. 所有价格为**官方收盘价**，不复权、不含手续费。

## 条例 06 · 校勘（诚实边界）

1. **归档未覆盖的年代一律留空**，并写明「当时币安尚无该交易对」——
   绝不用后世价格顶替（这是本作品最要紧的一条）；
2. 顺延取价必须显示顺延天数；
3. 交易日 ≠ 自然日（后 5 / 30 个交易日约合 7 / 43 个自然日）；
4. 事件库为人工整理，行情为官方数据，两类来源分列；
5. 史评样本小（各分组 2~24 条），只描述历史，不推未来；
6. 新闻抓取失败时如实提示「未抓取到匹配新闻」，不编造资讯；
7. 不提供下单、账户、资金、价格预测与买卖建议。

## 条例 07 · 使用（命令）

```bash
# 修编年史
python agent.py chronicle BTC                        # 默认通道
python agent.py chronicle ETH --official             # 纯档案模式（不需要上网工具）
python agent.py chronicle ETH --from 2021 --to 2022  # 限定年份
python agent.py chronicle BTC --category 监管政策     # 只看某一类
python agent.py chronicle BTC --no-quote             # 不取当期行情（纯历史）
python agent.py chronicle BTC --json                 # stdout 只输出 JSON

# 原有七条命令（用法不变）
python agent.py list | timeline BTC | news SOL | price BNB | report BTC | ask "…" | web

# 自然语言
python agent.py ask "给比特币修一卷编年史"
```

参数：`--live` / `--skill` / `--official`（三选一，互斥）、`--json`。

> `web` 的端口可用 `--port` 指定（默认 8000），也可用环境变量 `PORT`。

## 条例 08 · 版本记录

**v1.1.0（本次改造）**

1. 新增 `chronicle` 命令与 `chronicle.py` 编年史引擎：
   官方归档取数（按月缓存）→ 事件对齐 → 史评统计；
2. 新增四通道旗标（`--live` / `--skill` / `--official` / `--json`），所有子命令通用；
   其中 `--official` 为纯档案模式（不请求实时接口，用官方归档最近交易日）；
3. `price` 命令支持通道（`--skill` 由官方 CLI 直取、`--official` 用归档收盘）；
4. `ask` 自然语言新增「编年史」意图（说「编年」或「修史」即修一卷）；
5. 新增 `skills/biannian-shi/SKILL.md`（官方格式技能定义）。

> **原有七条命令、网页控制台、报告生成逻辑未作删改**，用法保持兼容；
> 本次只新增文件与新增能力（`agent.py` 的改动为新增 + 少量参数管线）。

**v1.0.0** 初版：事件库 + 网页控制台 + 新闻 + 行情 + 报告 + 自然语言提问。

## 条例 09 · 免责

本项目仅供学习研究，历史事件与行情数据均来自公开来源，不构成任何投资建议。
